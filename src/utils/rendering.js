// Shared canvas rendering utilities
// Used by all dish draw() functions

// Draw a metaball-style organic blob at (x, y) with given radius
export function drawBlob(ctx, x, y, radius, color) {
  // TODO: implement in Phase 3
}

// Start a breathing pulse animation loop on a canvas
// Returns a cancel function: const cancel = startBreathing(canvas, drawFn)
export function startBreathing(canvas, drawFn) {
  // TODO: implement in Phase 3
  return () => {}
}

// Draw slow-drifting speckle particles across the canvas
export function drawParticles(ctx, width, height, color, count = 40) {
  // TODO: implement in Phase 3
}
