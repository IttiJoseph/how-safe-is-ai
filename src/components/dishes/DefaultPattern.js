// DefaultPattern: organic metaball blobs — fallback for all dish categories
// draw(canvas, categoryData, options) → renders to the provided canvas element
export function draw(canvas, categoryData, options = {}) {
  // TODO: implement in Phase 3
}
